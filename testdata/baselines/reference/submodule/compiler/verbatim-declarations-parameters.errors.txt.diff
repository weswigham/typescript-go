--- old.verbatim-declarations-parameters.errors.txt
+++ new.verbatim-declarations-parameters.errors.txt
@@= skipped -0, +0 lines =@@
-<no content>
+verbatim-declarations-parameters.ts(16,5): error TS2322: Type '{}' is not assignable to type 'not "dummy" & { [x: string]: any; }'.
+  Type '{}' is not assignable to type 'not "dummy"'.
+
+
+==== verbatim-declarations-parameters.ts (1 errors) ====
+    type Map = {} & { [P in string]: any }
+    type MapOrUndefined = Map | undefined | "dummy"
+    export class Foo {
+      constructor(
+        // Type node is accurate, preserve
+        public reuseTypeNode?: Map | undefined,
+        public reuseTypeNode2?: Exclude<MapOrUndefined, "dummy">,
+        // Resolve type node, requires adding | undefined
+        public resolveType?: Map,
+      ) { }
+    }
+    
+    export function foo1(
+        // Type node is accurate, preserve
+        reuseTypeNode: Map | undefined = {},
+        reuseTypeNode2: Exclude<MapOrUndefined, "dummy">  = {},
+        ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+!!! error TS2322: Type '{}' is not assignable to type 'not "dummy" & { [x: string]: any; }'.
+!!! error TS2322:   Type '{}' is not assignable to type 'not "dummy"'.
+        // Resolve type node, requires adding | undefined
+        resolveType: Map = {}, 
+        requiredParam: number) {
+    
+    }
+    